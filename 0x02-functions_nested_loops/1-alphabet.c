#include "main.h"

/**
* main - Entry point
* print alphabet in lower case and a new line
* Return: Always 0 (Success)
*
*/

void print_alphabet(void)
{
	char(ch);
	for (ch = 'a' ; ch <= 'z' ; ch++)
{
	_putchar(ch);
}
	_putchar('\n');

}
