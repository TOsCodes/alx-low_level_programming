 #include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	char alpha[] = "abcdfghijklmnoprstuvwxyz";
	int dafe;

	for (dafe = 0; dafe < 24; dafe++)
	putchar(alpha[dafe]);
	putchar('\n');

return (0);
}
